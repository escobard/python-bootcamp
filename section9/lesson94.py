# nesting a list in a dictionary
travel_log = {
  "France": ["Paris", 'Lille', 'Dijon']
}

## dictionaries can only be called with keys, not numbers, for example print below will throw an error
## print(travel[0])